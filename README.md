# FastAPI CRUD Project

A simple CRUD (Create, Read, Update, Delete) project using FastAPI, PostgreSQL, and SQLAlchemy.

## How to Run

1. Install dependencies:
pip install -r requirements.txt

2. Run the server:
uvicorn main:app --reload

